using Godot;

namespace F00F.Vehicles.Tests
{
    public partial class Vehicles : Node3D
    {
        public override void _Ready()
        {
            //var scale = Scale.Z;
            //if (scale is 1) return;
            //var vehicles = GetChildren()
            //    .Select(x => (object)x.Get("Config") as VehicleData)
            //    .Where(x => x is not null);
            //vehicles.ForEach(x => x.Model.Scale = scale);
        }
    }
}
